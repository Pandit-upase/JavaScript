// Create object
const user = {

    name: "Pandit",
    age: 20

};


// Create function
// userObj is parameter
function printUser(userObj){

    // Print object name
    console.log(userObj.name);

    // Print object age
    console.log(userObj.age);

}


// Pass user object into function
// user is argument
printUser(user);